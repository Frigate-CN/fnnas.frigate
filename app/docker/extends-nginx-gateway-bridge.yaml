services:
  nginx:
    container_name: frigate-nginx
    image: nginx:latest
    user: root
    restart: unless-stopped
    volumes:
      - ${TRIM_APPDEST}/docker/nginx.conf:/etc/nginx/nginx.conf:ro
      - ${TRIM_APPDEST}:/var/run/fn-app
