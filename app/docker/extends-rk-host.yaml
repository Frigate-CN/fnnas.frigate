services:
  frigate:
    network_mode: host
    devices:
      - /dev/dri:/dev/dri # 用于GPU硬件加�?
      - /dev/dma_heap # RockChip DMA �?
      - /dev/rga # RockChip RGA
      - /dev/mpp_service # RockChip MPP 服务
    security_opt:
      - apparmor=unconfined
      - systempaths=unconfined
    volumes:
      - /sys/:/sys/:ro
