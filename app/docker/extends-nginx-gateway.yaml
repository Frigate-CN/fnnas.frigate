version: "3.8"

services:
  nginx:
    container_name: frigate-nginx
    image: nginx:latest
    user: root
    restart: unless-stopped
    volumes:
      - ${TRIM_APPDEST}/docker/${wizard_nginx_conf:-nginx.conf}:/etc/nginx/nginx.conf:ro
      - ${TRIM_APPDEST}:/var/run/fn-app
